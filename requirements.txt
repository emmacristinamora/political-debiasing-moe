# conda env => lt-proj

# works best with python 3.12

# === base ML ===
torch
numpy>=1.24
pandas>=2.0

# === LLM loading + training ===
transformers>=4.41.0
accelerate>=0.30.0
safetensors
huggingface_hub
sentencepiece
peft                    # lora expert fine-tuning — the main addition

# === data ===
datasets>=2.19.0

# === steering vectors ===
scikit-learn            # logistic probe + pca — the other main addition


# === config + utils ===
pyyaml>=6.0
tqdm>=4.66

# === evaluation ===
sentence-transformers
matplotlib>=3.8
# external llm-as-judge (src/12_judge_evaluation.py) uses a local Llama instruct
# model via transformers — no extra dependency beyond the LLM-loading block above